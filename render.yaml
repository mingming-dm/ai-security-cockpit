services:
  - type: web
    name: ai-security-cockpit
    env: node
    region: singapore
    buildCommand: cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build
    startCommand: cd backend && node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: AI_PROVIDER
        value: demo
